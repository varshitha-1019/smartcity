# Copy this file to .env (or .env.local) and adjust if needed.
#
# Must point at the same port the backend is actually running on
# (see backend/.env's PORT - defaults to 5000). If you change the backend's
# PORT, update this to match, or API requests will silently fail to reach
# the backend.
VITE_API_URL=http://localhost:5000/api
